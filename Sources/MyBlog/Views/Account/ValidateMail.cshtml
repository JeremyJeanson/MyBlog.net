@model System.Boolean
@using (Html.BeginPanel(Resources.EmailValidationSubject))
{
    if (Model)
    {
        <div class="alert alert-success">@Resources.EmailValidationStatusOk</div>
    }
    else
    {
        <div class="alert alert-danger">@Resources.EmailValidationStatusKo</div>
    }
}
